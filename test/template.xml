<ul +a=1 +x=0>
	<input !value="int" !bind="$value" />
	<li !value="a" !enum>
		<input !value="b" !bind="$value" />
		<ul>
			<li !value="c" !fragment>
				<li !enum +b="1" +k="++x">
				<div !fragment !text="k"></div>
					<button @click="a+=1" @keydown.key:KeyQ:KeyA.-repeat="a+=1">a+1:<span !fragment !text="a"></span></button>
					<button @click="b+=1">b+1:<span !fragment !text="b"></span></button>
					<div !fragment !text="$no"/>. <div !fragment !text="d">1</div> <div !fragment !text>1</div>
					<button @click="$remove()">移除</button>
					<button @click="$upMove()" :disabled="!$upMovable">上移</button>
					<button @click="$downMove()" :disabled="!$downMovable">下移</button>
				</li>
				<li><button @click="$add()">添加</button><button @click="$add({d: $length + 1})">添加(<span !fragment !text="$length + 1"></span>)</button></li>
			</li>
			<li !value="x" !enum +b=x$length +a=2>
				<button @click="a+=1">a+1:<span !fragment !text="a"></span></button>
				<button @click="b+=1">b+1:<span !fragment !text="b"></span></button>
					<input !bind=a type=number />
				<div !fragment !text="$no"/>. <div !fragment !text="d">1</div> <div !fragment !text>1</div>
			</li>
		</ul>
	</li>
</ul>
<ul>
	<input !value=int !bind="$value" />
	<li !value="a" !enum>
		<input !value="b" !bind="$value" />
		<ul>
			<li !value="c" !enum>
				<input !value="d" !bind="$value" />
				<input !bind="$value" />
			</li>
			<li !value="x" !enum>
				<input !value="d" !bind="$value" />
				<input !bind="$value" />
			</li>
		</ul>
	</li>
</ul>
